import 'package:flutter/material.dart';

class AppColors {
  // Common
  static const white = Colors.white;
  static const black = Colors.black;
  // Light
  static const lightBackground = Color(0xFFF5F5F5);
  static const lightPrimary = Color(0xFF3F51B5);
  static const lightText = Color(0xFF212121);
  static const lightSubText = Color(0xFF757575);
  // Dark
  static const darkBackground = Color(0xFF121212);
  static const darkPrimary = Color(0xFF90CAF9);
  static const darkText = Color(0xFFFFFFFF);
  static const darkSubText = Color(0xFFBDBDBD);
}
